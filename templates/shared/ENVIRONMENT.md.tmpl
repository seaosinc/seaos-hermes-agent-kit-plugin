## この作業環境について

全役割が共通して知っておくべき、この PC の事実。**hermes-kit init が実測して生成した。**

### マシン

- {{OS_NAME}} {{OS_VERSION}} / {{ARCH}}
- シェルは `{{SHELL}}`。ユーザーは `{{USER}}`、ホームは `{{HOME}}`
{{PKGMGR_LINE}}

### 開発リポジトリの置き場

**`{{REPO_ROOT}}` に開発リポジトリが並んでいる**（{{REPO_DIRS}} ディレクトリ、うち {{REPO_GITS}} が git リポジトリ）。
実装や調査の対象は、ほぼここにある。リポジトリを指定されたら、まずこの下を探す。

`--workspace worktree:{{REPO_ROOT}}/<repo>` のように指定して作業する。

### 言語ランタイム（踏みやすい落とし穴）

{{PYTHON_LINE}}
{{NODE_LINE}}
- テストランナーやビルドツールが入っていない環境もある。走らせる前に存在を確認すること

### 委譲先の CLI

{{CLAUDE_LINE}}
{{OPENCODE_LINE}}

### リポジトリ固有の情報

リポジトリのルートに `AGENTS.md` があれば、**そこで作業するとき自動で読み込まれる**
（CWD から解決される）。リポジトリ固有の規約やビルド手順はそちらに書く。
このファイルには書かない。
